name: "Simple input object"
description: "Tests planning with a simple input object argument"

query: |
  query GetUsers($filter: UserFilter) {
    users(filter: $filter) {
      id
      name
      email
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: users
      operation: "query($filter: UserFilter) { users(filter: $filter) { id name email } }"
      dependsOn: []
      repeatedExecution: false
