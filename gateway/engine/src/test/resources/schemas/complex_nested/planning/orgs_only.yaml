name: "Organizations only"
description: "Query only organization and team fields - no lookups"

query: |
  {
    organizations {
      name
      teams {
        teamName
      }
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: orgs
      operation: "{ organizations { name teams { teamName } } }"
      dependsOn: []
      repeatedExecution: false
