name: "Inline fragment with cross-subgraph fields"
description: "Tests planning where inline fragment fields come from different subgraph"

query: |
  {
    contents {
      id
      title
      ... on Article {
        views
        readTime
      }
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: content
      operation: "{ contents { id title __typename } }"
      dependsOn: []
      repeatedExecution: false
    - id: 2
      subgraph: analytics
      operation: "query($id: ID!) { contentById(id: $id) { ... on Article { views readTime } } }"
      dependsOn: [1]
      requirements:
        id: "id"
      repeatedExecution: true
