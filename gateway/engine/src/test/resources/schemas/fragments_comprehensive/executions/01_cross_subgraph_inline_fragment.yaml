name: "Cross-subgraph inline fragment execution"
description: "Tests inline fragment with fields from content and analytics subgraphs"

query: |
  {
    contents {
      id
      title
      ... on Article {
        body
        views
        readTime
      }
    }
  }

variables: {}

subgraphCalls:
  - subgraph: content
    operation: '{ contents { id title __typename ... on Article { body } } }'
    variables: {}
    response: {
      data: {
        contents: [
          { __typename: "Article", id: "a1", title: "Tech News", body: "Article content here" },
          { __typename: "Video", id: "v1", title: "Tutorial Video" }
        ]
      }
    }

  - subgraph: analytics
    operation: 'query ($id: ID!) { contentById(id: $id) { ... on Article { views readTime } } }'
    variables: { id: "a1" }
    response: { data: { contentById: { __typename: "Article", views: 1500, readTime: 180 } } }

  - subgraph: analytics
    operation: 'query ($id: ID!) { contentById(id: $id) { ... on Article { views readTime } } }'
    variables: { id: "v1" }
    response: { data: { contentById: { __typename: "Video" } } }

expectedResponse: {
  data: {
    contents: [
      { id: "a1", title: "Tech News", body: "Article content here", views: 1500, readTime: 180 },
      { id: "v1", title: "Tutorial Video" }
    ]
  }
}
