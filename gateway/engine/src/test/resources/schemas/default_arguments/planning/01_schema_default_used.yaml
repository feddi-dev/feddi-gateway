name: "Schema default used"
description: "Tests planning when schema-defined default arguments are used"

query: |
  query Search($q: String!) {
    search(query: $q) {
      id
      title
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: search
      operation: "query($q: String!) { search(query: $q) { id title } }"
      dependsOn: []
      repeatedExecution: false
