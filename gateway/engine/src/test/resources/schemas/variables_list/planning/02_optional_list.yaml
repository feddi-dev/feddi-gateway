name: "Optional list variable"
description: "Tests planning with optional list [String]"

query: |
  query GetProductsByTags($tags: [String]) {
    productsByTags(tags: $tags) {
      id
      name
      tags
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: products
      operation: "query($tags: [String]) { productsByTags(tags: $tags) { id name tags } }"
      dependsOn: []
      repeatedExecution: false
