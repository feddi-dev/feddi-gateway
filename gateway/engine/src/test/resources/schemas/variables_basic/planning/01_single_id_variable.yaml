name: "Single ID variable"
description: "Tests planning with a single ID type variable"

query: |
  query GetUser($userId: ID!) {
    user(id: $userId) {
      id
      name
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: users
      operation: "query($userId: ID!) { user(id: $userId) { id name } }"
      dependsOn: []
      repeatedExecution: false
