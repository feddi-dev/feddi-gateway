name: "Required variable"
description: "Tests planning with required non-null variable"

query: |
  query GetUser($userId: ID!) {
    user(id: $userId) {
      id
      name
      age
      active
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: users
      operation: "query($userId: ID!) { user(id: $userId) { id name age active } }"
      dependsOn: []
      repeatedExecution: false
