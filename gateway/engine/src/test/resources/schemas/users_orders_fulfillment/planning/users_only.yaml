name: "Only user fields"
description: "Query only from users subgraph"

query: |
  {
    users {
      id
      name
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: users
      operation: "{ users { id name } }"
      dependsOn: []
      repeatedExecution: false
