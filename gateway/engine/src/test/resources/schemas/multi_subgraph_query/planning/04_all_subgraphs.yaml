name: "All subgraphs in one query"
description: "Query fields from all three subgraphs in a single query - key test for unified root"

query: |
  {
    products {
      id
      name
    }
    reviews {
      id
      text
    }
    users {
      id
      name
    }
  }
