name: "Users only"
description: "Query only users field from users subgraph - tests unified root reaches all subgraphs"

query: |
  {
    users {
      id
      name
      email
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: users
      operation: "{ users { id name email } }"
      dependsOn: []
      repeatedExecution: false
