name: "Only local fields"
description: "Query only fields from products subgraph - no lookup needed"

query: |
  {
    products {
      id
      name
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: products
      operation: "{ products { id name } }"
      dependsOn: []
      repeatedExecution: false
