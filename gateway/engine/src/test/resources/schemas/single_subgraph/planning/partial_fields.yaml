name: "Partial fields"
description: "Only some user fields"

query: |
  {
    users {
      name
      email
    }
  }

expectedPlan:
  steps:
    - id: 1
      subgraph: main
      operation: "{ users { name email } }"
      dependsOn: []
      repeatedExecution: false
